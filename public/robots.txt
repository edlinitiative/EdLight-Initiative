# Robots.txt for EdLight Initiative
# https://www.edlight.org/robots.txt

# Allow all search engines to crawl the site
User-agent: *
Allow: /

# Disallow admin areas.
#
# Both the bare path and the subtree. "Disallow: /admin/" alone does not match
# "/admin", which is the actual route — so the dashboard was crawlable, and it
# was serving "index, follow" besides. It now carries a noindex of its own in
# app/admin/layout.tsx; this is the belt to that braces.
Disallow: /admin
Disallow: /admin/
Disallow: /api/admin
Disallow: /api/admin/

# Sitemap location
#
# This must be the www host. It declared the apex — https://edlight.org —
# which 307-redirects to www, so the one URL whose job is to tell a crawler
# where the sitemap is was itself a redirect. lib/site.ts holds the same
# canonical origin for the sitemap, canonical tags, and JSON-LD.
Sitemap: https://www.edlight.org/sitemap.xml

import ContactContent from "./ContactContent";

export const metadata = {
    title: "Contact EdLight Initiative",
    description:
        "Connect with the EdLight team. Reach out for partnerships, media inquiries, program support, or general questions about our education initiatives.",
};

export default function ContactPage() {
    return <ContactContent />;
}
